const Form = () => {
  return (
    <div>
      <form>
        <label htmlFor="title">Title</label>
        <input type="text" name="title"></input>
      </form>
    </div>
  );
};

export default Form;
